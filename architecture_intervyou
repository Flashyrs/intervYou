# IntervYou Architecture & Systems Design Reference

This document provides a detailed breakdown of the architectural decisions, trade-offs, distributed systems patterns, optimizations, and technologies implemented within the IntervYou collaborative interview platform.

---

## 1. Technology Stack Overview

| Component | Technology | Role & Benefit |
| :--- | :--- | :--- |
| **Core Framework** | Next.js (App Router, API Routes) | Provides server-side rendering (SSR), search engine optimization (SEO), and dynamic API route handling. |
| **Realtime Signaling** | Supabase WebSockets | Handles live messaging channels and presence coordination without hosting dedicated socket servers. |
| **Keystroke Sync** | Yjs CRDT + `y-webrtc` (P2P) + `y-monaco` | Coordinates conflict-free live document editing via Peer-to-Peer WebRTC, with a fail-safe fallback to Supabase Realtime. |
| **Media Engine** | LiveKit Cloud SFU + WebRTC Fallback (P2P + Google/Twilio STUN) | Powers optimized multi-stream video, audio, and screen-share using LiveKit Cloud, with an automatic fallback to P2P WebRTC using Google/Twilio STUN servers if LiveKit is offline. |
| **Collaborative Whiteboard** | Excalidraw + Supabase Realtime | Provides a shared sketching board with real-time canvas element sync over dedicated Supabase whiteboard channels. |
| **Database** | PostgreSQL + Prisma ORM | Stores persistent user data, session histories, configurations, and submission records. |
| **Caching Layer** | Redis (`ioredis`) | Holds ephemeral, high-frequency session backups to support fast restoration after dropouts. |
| **AI Orchestration** | Gemini API (2.5 Flash / Lite) | Generates dynamic problems, skeletons, dynamic test cases, and reflection drivers. |
| **Code Execution** | Piston API + Judge0 Fallback | Executes untrusted user code using Piston API with an automatic fallback to Judge0 sandboxes. |

---

## 2. Distributed Systems Decisions & Trade-offs

### A. Peer-to-Peer (P2P) WebRTC Signaling over Supabase WebSockets
* **Approach:** Rather than developing, scaling, and maintaining a custom WebSocket signaling cluster (such as Socket.io or ws on Node.js), signaling is managed by broadcasting SDP offers, SDP answers, and ICE candidates over the existing Supabase Realtime Channels.
* **Trade-offs:**
  * **Pros:** Eliminates infrastructure management and hosting costs; simplifies routing topology to a single, unified channel.
  * **Cons:** Strictly bound by Supabase free-tier rate limits. If the channel is flooded by cursor tracking, signaling packets lag, leading to delayed connections.
  * **Mitigation:** Cursor and whiteboard broadcasts are aggressively debounced to stay under the 10 messages/second threshold.

### B. LiveKit SFU with Dual-PeerConnection WebRTC Fallback
* **Approach:** The platform uses a hybrid media engine in [VideoCall.tsx](file:///e:/Local/javascript/interv/components/VideoCall.tsx):
  1. **Primary Media Engine (LiveKit Cloud SFU):** By default, the app utilizes LiveKit Cloud. An SFU (Selective Forwarding Unit) topology handles multi-stream audio/video and screen share. This provides optimal server-side bandwidth allocation, high quality, and congestion control.
  2. **Fallback Media Engine (P2P WebRTC):** If LiveKit credentials are empty or the socket fails to fetch a token from `/api/livekit/token`, the app falls back to pure Peer-to-Peer WebRTC. The fallback topology mounts two distinct peer connections: `pcRef` (main camera/mic call) and `screenSharePcRef` (screen sharing) to separate congestion control logic.
  3. **STUN/ICE Infrastructure:** The P2P fallback coordinates ICE candidate gathering using a pool of high-availability STUN servers, specifically **Google STUN** (`stun:stun.l.google.com:19302`, etc.) and **Twilio STUN** (`stun:global.stun.twilio.com:3478`).
* **Trade-offs:**
  * **Pros:** Highly resilient video infrastructure. Production users get a premium SFU experience with LiveKit, while experiencing zero downtime because of the seamless, server-free direct P2P fallback.
  * **Cons:** Managing dual-engine routing logic adds complexity to browser stream hooks and WebRTC signaling handshakes.

### C. Client-Side CRDT (Yjs) with Hybrid P2P & Relay Transports
* **Approach:** Collaborators type concurrently into the Monaco Editor with conflicts resolved client-side using Yjs CRDTs. The sync pipeline utilizes a hybrid transport setup:
  1. **Primary Transport (P2P WebRTC):** The editor first attempts to establish direct Peer-to-Peer synchronization using `y-webrtc` over public signaling servers. This gives the lowest latency sync and consumes zero server resources.
  2. **Fallback Transport (Supabase Realtime Broadcast):** If a P2P connection cannot be established within 4 seconds (e.g. due to restrictive corporate firewalls or NATs), the system triggers a fallback to sync CRDT updates via a custom `YjsSupabaseProvider` over Supabase Realtime broadcast channels.
* **Trade-offs:**
  * **Pros:** Provides high resilience. Collaborators get ultra-fast peer-to-peer sync when possible, with a reliable serverless cloud relay (Supabase) if direct P2P connection fails. No centralized OT server coordination is required.
  * **Cons:** WebRTC signaling fallback introduces a potential 4-second delay window if the direct connection fails before migrating to Supabase Realtime. Exchanging complete state histories requires a two-step sync protocol (`sync-step-1` vectors and `sync-step-2` updates).

### D. Resilient Code Execution Pipeline (Piston + Judge0 Fallback)
* **Approach:** Code compilation and execution inside the room is handled by a multi-layered runner pipeline in [lib/codeExecutor.ts](file:///e:/Local/javascript/interv/lib/codeExecutor.ts):
  1. **Primary Runner (Piston API):** If enabled, it first routes execution to Piston (`https://emkc.org/api/v2/piston`) with a fast **6-second guarded timeout** (`AbortController`).
  2. **Fallback Runner (Judge0):** If Piston is disabled, fails, throws a compiler/server error, or times out, the system automatically falls back to execute the code via the Judge0 execution engine.
* **Trade-offs:**
  * **Pros:** Highly fault-tolerant. Protects candidates from interview interruptions if one of the sandbox runtimes is offline or rate-limiting.
  * **Cons:** Increases configuration complexity (managing separate environment keys and URLs for both Piston and Judge0).

---

## 3. Concurrency Control & State Management

```mermaid
graph TD
    ClientA[Client A] -- Keystrokes --> Yjs[Yjs CRDT Engine]
    ClientA -- State Changes --> Broadcast[Supabase Broadcast 400ms Debounce]
    Broadcast --> ClientB[Client B]
    ClientA -- State Backup --> Persist[Redis POST 1s Debounce]
    Persist --> Redis[(Redis Cache)]
    Redis --> Postgres[(PostgreSQL Backup)]
    
    style Yjs fill:#4f46e5,stroke:#312e81,color:#fff
    style Redis fill:#dc2626,stroke:#7f1d1d,color:#fff
    style Postgres fill:#0284c7,stroke:#0c4a6e,color:#fff
```

### A. Two-Tier State Replication (Redis Cache + Postgres DB)
* **Approach:** To handle high-concurrency state writes, non-editor modifications (timer toggles, active problem descriptions, language selections) are debounced and persisted to Redis with a 24-hour TTL ([lib/interviewStateStore.ts](file:///e:/Local/javascript/interv/lib/interviewStateStore.ts)). Snapshots are asynchronously written to PostgreSQL.
* **Trade-offs:**
  * **Pros:** Users get instant recovery after network dropouts or reloads. Redis acts as a fast cache-aside data source, protecting the primary SQL database from write starvation.
  * **Cons:** Eventual consistency between Redis cache states and Postgres DB state snapshots must be managed manually.

### B. Optimistic Concurrency Control (OCC) with Field-Level Versioning
* **Approach:** To prevent concurrent users from overwriting shared settings, state changes pass a `baseVersion`. The server compares it to individual field version counters inside `__meta.fieldVersions` before merging patches.
* **Trade-offs:**
  * **Pros:** Eliminates the latency of database row locking. Because version checks are field-specific, two users editing separate states (e.g., updating compiler flags vs. freezing editor workspace) do not block each other.
  * **Cons:** If a conflict happens (HTTP `409`), the client must abort the local transaction and execute a full reload.

### C. Distributed Mutex via Redis `set NX`
* **Approach:** During lobby matchmaking, competing users might click to accept the same lobby invite. The API in [app/api/random/accept/route.ts](file:///e:/Local/javascript/interv/app/api/random/accept/route.ts) locks claims atomically:
  ```typescript
  const claimed = await redis.set(key, userId!, "EX", 3600, "NX");
  ```
* **Benefit:** Ensures that exactly one interviewer can pair with a waiting candidate, responding with a `409` conflict to late-binding clients to protect room integrity.

### D. Isolated Canvas Element Sync (Excalidraw Whiteboard)
* **Approach:** The whiteboard panel ([components/interview/WhiteboardPanel.tsx](file:///e:/Local/javascript/interv/components/interview/WhiteboardPanel.tsx)) uses Excalidraw for sketching. To prevent canvas updates (which contain large arrays of vector shapes) from blocking or delaying critical cursor/code synchronization:
  1. **Channel Isolation:** It separates whiteboard traffic onto a dedicated Supabase channel (`whiteboard-${sessionId}`) using [lib/whiteboardRealtime.ts](file:///e:/Local/javascript/interv/lib/whiteboardRealtime.ts).
  2. **Debounced Sync:** It queues and debounces canvas updates to minimize network packet spam.
* **Benefit:** Ensures whiteboard activity has zero performance footprint on real-time code typing and audio/video calling streams.

---

## 4. Multi-Threading, Tasking & Scheduling

### A. Non-Blocking Judge0 Integration with Guarded Timeouts
* **Approach:** Code compilations are routed to Judge0. To keep the Next.js API thread pool from locking up under infinite loops, runtime processes are wrapped in `AbortController` timeouts of 25 seconds:
  ```typescript
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), 25000);
  ```
* **Benefit:** Reclaims execution resources and socket threads if client codes execute infinite loops or consume excessive execution resources.

### B. Delayed Graceful Session Expiration Check
* **Approach:** WebSocket dropouts are common due to brief network interruptions. The system does not immediately close active interview rooms upon receipt of a `LEFT` presence event. Instead, it utilizes a **30-second Grace Period** ([lib/sessionExpiration.ts](file:///e:/Local/javascript/interv/lib/sessionExpiration.ts)):
  1. Captures participant events in a `ParticipantEvent` log.
  2. Evaluates if all historical users are marked as `LEFT`.
  3. Executes cleanup and fires archive notification emails only if the 30-second delay expires without any user rejoining.

---

## 5. Performance & Network Optimizations

### A. Supabase Performance Shield (Throughput Debouncing)
* **Problem:** Free tier Supabase sockets drop connections or fallback to slow REST calls if a client broadcasts more than 10 events/second.
* **Solution:** Buffers high-frequency mouse actions and text sync events:
  * **Cursor broadcats:** Debounced to `400ms` ([hooks/useInterviewState.ts](file:///e:/Local/javascript/interv/hooks/useInterviewState.ts#404-416)).
  * **State updates:** Debounced to `400ms` ([hooks/useInterviewState.ts](file:///e:/Local/javascript/interv/hooks/useInterviewState.ts#424-448)).
* **Benefit:** Keeps traffic well under socket rate-limits while keeping the signaling line clear for WebRTC candidate exchanges.

### B. AI Enhance Failover and Fallback Pipelines
* **Optimization:** Gemini enhancement queries utilize a failover array model pipeline ([app/api/ai/enhance/route.ts](file:///e:/Local/javascript/interv/app/api/ai/enhance/route.ts#41-88)). It attempts queries with the faster, low-cost `gemini-2.5-flash-lite` first. If it encounters a timeout (60-second limit) or rate limit, it falls back to `gemini-2.5-flash`.
* **JSON Repair:** If the LLM generates conversational text around its JSON code fences, the route executes a regex bracket-matching fallback:
  ```typescript
  const start = cleanJson.indexOf('{');
  const end = cleanJson.lastIndexOf('}');
  const aiResult = JSON.parse(cleanJson.substring(start, end + 1));
  ```

### C. Reflection-Based Dynamic Java Test Driver
* **Optimization:** Standard compilers require pre-compiled library wrappers (like JSON JARs) to parse input test JSON inside sandbox environments. 
* **Solution:** The build engine compiles a **native custom JSON parser** polyfill directly into the Java compilation script dynamically ([lib/interviewUtils.ts](file:///e:/Local/javascript/interv/lib/interviewUtils.ts#134-523)). It pairs this with a Java Reflection test harness that maps inputs to target parameters at runtime.
* **Benefit:** Allows rapid test suites execution in Judge0 with 0 extra library dependencies or classpath setups.

### D. lazyConnect on ioredis
* **Optimization:** Initializes Redis with `lazyConnect: true` ([lib/redis.ts](file:///e:/Local/javascript/interv/lib/redis.ts#10)).
* **Benefit:** Prevents compilation crashes during serverless/static Next.js deployments where Redis connection strings are not yet active or ports are unreachable.

### E. Submission Table Upsert
* **Optimization:** Submission keys enforce a composite uniqueness constraint:
  ```prisma
  @@unique([sessionId, problemId, userId])
  ```
* **Benefit:** Submissions write using Prisma's upsert API. This prevents unbounded database row growth and ensures performance logs remain clean and fast.

### F. Performance Prober Shift+D Overlay
* **Feature:** Monitors synchronization p50, p95, p99 latencies, messages/sec throughput, and failure rates via continuous ping-pongs ([hooks/usePerformanceProber.ts](file:///e:/Local/javascript/interv/hooks/usePerformanceProber.ts)).
* **Benefit:** Empowers interviewers and candidates to monitor stream stability, identifying local ISP congestion versus platform issues.

---

## 6. Submissions, History & Retrieval Optimizations

To handle high-scale data retrieval and prevent database or network bottlenecks, we implemented database indexing, selective query projection, and dynamic on-demand loading.

### A. Selective Query Projection & Payload Size Reduction
* **Problem:** Originally, fetching the list of 50 submissions via `GET /api/submissions` loaded the entire user-written `code` block (up to 10KB+ per file) and all detailed `testResults` (including stdout, error traces, and expected/actual outputs for all test cases). This resulted in multi-megabyte JSON payloads and heavy database serialization.
* **Solution:** Configured the listing API in [app/api/submissions/route.ts](file:///e:/Local/javascript/interv/app/api/submissions/route.ts) to use Prisma `select` projection:
  * Omit the large `code` text field from list queries.
  * Omit large test run diagnostics (stdout, expected/actual outputs, errors) from nested `testResults` queries, fetching only the lightweight `passed` boolean status.
* **Benefit:** Reduces list retrieval payloads by over 95%, speeding up network transit and JSON parsing times on the client.

### B. On-Demand Submission Details Loading
* **Approach:** To retain full detail viewing capabilities without the payload bloat:
  * Added a single-submission fetch endpoint: `GET /api/submissions?id=<submissionId>` which returns the complete record (full code and detailed test logs).
  * Updated client-side states in [app/dashboard/page.tsx](file:///e:/Local/javascript/interv/app/dashboard/page.tsx), [app/submissions/page.tsx](file:///e:/Local/javascript/interv/app/submissions/page.tsx), and [app/dashboard/submissions/page.tsx](file:///e:/Local/javascript/interv/app/dashboard/submissions/page.tsx) to lazy-load the submission code and detailed diagnostics on-demand only when a user triggers "Show Code" or clicks "View" details.

### C. Eliminate N+1 Secondary Notes Queries
* **Problem:** The system previously retrieved interviewer notes using a secondary, raw SQL query (`getInterviewerNotesForSessions`) catching dynamic errors for backward compatibility with older migrations. Running a second query for every list fetch added database roundtrip overhead.
* **Solution:** Now that migrations are finalized, `interviewerNotes` is fetched directly in the main `select` block of the primary queries in [app/api/submissions/route.ts](file:///e:/Local/javascript/interv/app/api/submissions/route.ts) and [app/api/history/route.ts](file:///e:/Local/javascript/interv/app/api/history/route.ts).
* **Benefit:** Eliminates secondary queries entirely, reducing history and submissions database roundtrips down to a single query execution.

### D. Targeted Database Indexes
* **Approach:** Added performance indexes to the PostgreSQL schema in [prisma/schema.prisma](file:///e:/Local/javascript/interv/prisma/schema.prisma) to cover the query filters and sort order:
  * `@@index([userId, updatedAt])` on `Submission`: Optimizes user submissions page list retrieval sorted by most recent first (`orderBy: { updatedAt: "desc" }`).
  * `@@index([createdBy, scheduledFor, createdAt])` and `@@index([inviteeEmail, scheduledFor, createdAt])` on `InterviewSession`: Prevents in-memory sorting or full-table scans for history/upcoming session lookups.

---

## 7. Performance Benchmarks & Load Capacity Case Study

To validate the scalability, fault tolerance, and throughput limits of the platform under high-concurrency conditions, we executed a load test simulation directly targeting the live Vercel and Supabase production environment.

### A. Benchmark Results

| Concurrency Level | Throughput | API Success Rate | p50 Sync Delay (1-way WS) | p50 API Latency (Roundtrip) | p95 API Latency |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **5 Rooms / 10 Users** | 28.6 updates/sec | **100% (490/490 OK)** | **44ms** | **293ms** | **489ms** |
| **50 Rooms / 100 Users** | 242.6 updates/sec | **100% (4763/4763 OK)** | **51ms** | **940ms** | **2161ms** |

### B. Architectural Accomplishments
1. **Zero Data Dropouts (100% Success Rate):** Across all runs, including the massive 100-user concurrent test executing **4,763 database writes** in 15 seconds, the platform maintained a **100% success rate** with zero request timeouts or connection failures.
2. **Blazing-Fast WebSockets:** Peak WebSocket message synchronization delays remained extremely low at **51ms (p50)** and **295ms (p99)**, ensuring collaborative cursor tracking, drawings, and typing continue to feel instantaneous to end-users even under heavy load.
3. **Connection Pool Optimization:** Transitioning to **Transaction-level pooling** (via Supavisor on port `6543`) solved connection exhaustion bottlenecks, allowing thousands of queries to share the database's free-tier pool size.

### C. Analysis of Latency Under Peak Load
While the average WebSocket sync delay is extremely low (51ms), the API roundtrip latency scales from **293ms** (at 10 users) to **940ms** (at 100 users). 
* **The Cause:** This latency scaling is completely normal for a Free Tier Supabase database instance under heavy resource constraints (constrained CPU/IOPS). As the database pooler queues 240+ database writes per second, transaction execution time increases.
* **Production Recommendation:** For enterprise scales exceeding 50 concurrent active rooms, upgrading from a Free Tier database to a Supabase Pro or Enterprise Tier database will resolve the database execution queue queueing bottleneck, dropping peak load latency back to <100ms.

